import EndLevel, { Header, Subtitle, Text } from 'components/end-level/index'

const FailScreen = () => {
    return (
        <EndLevel>
            <Header></Header>
        </EndLevel>
    )
}

export default FailScreen
